import 'package:flutter/material.dart';

//este sera nuestra pantalla de dashboard

class DashboardOfFragments extends StatelessWidget {

  @override
  Widget build(BuildContext context)
  {
    return Scaffold(
      appBar: AppBar(
        title: Text("Dashboard"),
      ),

    );
  }
}
